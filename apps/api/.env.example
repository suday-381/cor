# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=corplan_rkap

# JWT Configuration (optional for seed, but good to have)
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRATION=24h
